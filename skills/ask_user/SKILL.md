---
name: ask_user
description: Ask for a necessary user decision with the RAI ask-user block.
---

# Ask user

When a decision about scope, direction, language, style, or next steps is genuinely required, render one standalone `rai_ask_user` code block using the documented single-question or multi-question JSON shape. Do not use it for ordinary replies. Wait for all requested choices before proceeding.
