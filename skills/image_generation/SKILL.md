---
name: image_generation
description: Generate an image through the configured server-side image tool.
---

# Image generation

Call `generate_image` when the user asks to create an image. Supply only a clear image prompt and supported parameters. The server controls providers, image URLs, and display. Do not invent image links or expose provider endpoints, credentials, or tool-call syntax.
