Historical sanitized release archives for RAI.

Layout:
- Each dated folder is one archival upload batch.
- `release_manifest.csv` records version labels and chosen dates.
- `post_scan_report.json` records residual secret scan hits after sanitization.
- `archives/` contains the sanitized zip packages.

These packages are intended for historical preservation only, not for direct production deployment.
