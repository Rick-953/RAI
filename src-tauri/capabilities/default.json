{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows only the Tauri core defaults required by the RAI windows.",
  "windows": [
    "main",
    "quick"
  ],
  "permissions": [
    "core:default"
  ]
}
